No necesita instalación porque está en despliegue en render, pero si se quiere instalar sólo es clonar el repositorio y usar el siguiente comando en el cmd dentro de la raíz del proyecto:

```bash
node server.js
```
